/***************************************************\
| Assignmet#2                                       |
| 			                	    |
| Author:Wasim G Aswad		           	    |
| Student ID: 17193559                              |
| Last Change:15/11/2019    			    |
|					            |
\***************************************************/

#include "TwoDimensional.h"

double squareArea(double side){ //1 to find the aree of the square
double Area = S2M;
return Area;
}

double squarePerimeter(double side){ //1 to find the perimeter of the square
double Perimeter = 4 * side;
return Perimeter;
}

double rectangleArea(double length , double width){ //2 to find the area of the rectangle
double Area = length * width;
return Area;
}

double rectanglePerimeter(double length , double width){ //2 to find the perimeter of the rectangle
double Perimeter = ( 2 * length ) + ( 2 * width );
return Perimeter;
}

double circleArea(double radius){ //3 to find the area of the circle
double Area = PI * R2;
return Area;
}

double circleCircumference(double radius){ //3 to find the circumference of the circle
double Circumference = 2 * PI * radius;
return Circumference;
}

double triangleArea(double base, double height){ //4 to find the area of the triangle
double Area = (base * height) / 2 ;
return Area;
}

double trianglePerimeter(double side1, double side2, double side3){ //4 to find the perimeter of the triangle
double Perimeter = S3P;
return Perimeter;
}

double parallelogramArea(double base , double height){ //5 to find the area of the parallelogra
double Area = base * height;
return Area;
}

double parallelogramPerimeter(double base, double sideA){ //5 to find the perimeter of the parallelogra
double Perimeter = ( 2 * sideA ) + ( 2 * base );
return Perimeter;
}

